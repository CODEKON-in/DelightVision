Put imported images here (for example, the logo or hero photograph) and import
them in a component:

    import hero from "../assets/hero.jpg";

Files that should keep a stable, predictable URL instead — anything you want
to reference as `/photo-1.jpg` — belong in `public/` at the project root.
